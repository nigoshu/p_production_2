 <div class="main">
    <h1>ユーザー一覧</h1>
<%= users %>
    
    <p><%= @user.name %></p>
    <p><%= @user.email %></p>
    <div class="">
        <%= link_to("編集","/users/#{params[:id]}/edit") %>
    </div>
</div>